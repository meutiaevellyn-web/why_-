<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Why...???</title>

  <!-- Font & Style -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500&family=Playfair+Display:wght@500&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #f6f3ef;
      --card: #e8e3dc;
      --card-active: #fdfbf8;
      --text: #4a473d;
    }

    * {
      box-sizing: border-box;
      -webkit-tap-highlight-color: transparent;
    }

    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: var(--bg);
      color: var(--text);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      padding: 2rem 1rem;
    }

    h1 {
      font-family: 'Playfair Display', serif;
      font-size: clamp(1.5rem, 4vw, 2.3rem);
      color: #3c3a34;
      margin-bottom: 1.5rem;
      text-align: center;
      line-height: 1.3;
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
      gap: 1rem;
      max-width: 600px;
      width: 100%;
    }

    .card {
      position: relative;
      background: var(--card);
      border-radius: 18px;
      aspect-ratio: 1 / 1;
      cursor: pointer;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      overflow: hidden;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      touch-action: manipulation;
    }

    .card:hover {
      transform: scale(1.03);
      box-shadow: 0 4px 10px rgba(0,0,0,0.12);
    }

    .icon {
      font-size: clamp(2rem, 6vw, 2.8rem);
      opacity: 0.75;
      transition: opacity 0.3s ease;
      user-select: none;
    }

    input[type="checkbox"] {
      display: none;
    }

    .reason {
      position: absolute;
      inset: 0;
      background: var(--card-active);
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 1rem;
      border-radius: 18px;
      opacity: 0;
      transition: opacity 0.6s ease;
      font-size: clamp(0.9rem, 3.5vw, 1rem);
      font-weight: 400;
      line-height: 1.4;
    }

    input[type="checkbox"]:checked + .icon {
      opacity: 0;
    }

    input[type="checkbox"]:checked ~ .reason {
      opacity: 1;
    }

    footer {
      margin-top: 2rem;
      font-size: 0.8rem;
      opacity: 0.6;
    }

    @media (max-width: 480px) {
      .cards {
        gap: 0.8rem;
      }
      .card {
        border-radius: 15px;
      }
    }
  </style>
</head>
<body>
  <h1>Why You’re Awesome 🌸</h1>
  <div class="cards">
    <label class="card">
      <input type="checkbox">
      <div class="icon">🌷</div>
      <div class="reason">You always listen with a kind heart.</div>
    </label>

    <label class="card">
      <input type="checkbox">
      <div class="icon">🌿</div>
      <div class="reason">Your laughter makes the world brighter.</div>
    </label>


   

    <label class="card">
      <input type="checkbox">
      <div class="icon">🍦</div>
      <div class="reason">You stay genuine no matter what.</div>
    </label>
  </div>

  <footer>made with ❣ by your friend</footer>
</body>
</html>
